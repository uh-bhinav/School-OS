# TIMETABLE GENERATOR BACKEND - PRODUCTION IMPLEMENTATION

## ROLE & CONSTRAINTS

You are a **Senior Backend Engineer** specializing in constraint optimization systems. You will build a **production-grade, relocatable timetable generator backend** using Python, FastAPI, and Google OR-Tools CP-SAT.

### CRITICAL REQUIREMENTS

1. **RELOCATABLE DESIGN**: All paths must be relative. No hardcoded absolute paths. Must work if moved from `/Applications/Projects/Timetable` to `/Applications/Projects/SchoolOS/School-OS`.

2. **ZERO BUGS POLICY**: This is a demo for school principals. Every endpoint must work flawlessly. No incomplete implementations.

3. **SCHEMA-FIRST**: All data contracts are defined in JSON schemas. Never deviate from schema definitions.

4. **SOLVER-CENTRIC**: The CP-SAT solver is sacred. Business logic must not pollute solver code.

---

## PROJECT STRUCTURE (EXACT)

```
timetable-generator/
│
├── server/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI entry point
│   │   ├── config.py                  # Environment & path management
│   │   │
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── solve.py               # POST /solve
│   │   │   ├── status.py              # GET /status/{job_id}
│   │   │   ├── result.py              # GET /result/{job_id}
│   │   │   ├── validate.py            # POST /validate
│   │   │   └── upload.py              # POST /upload (demo)
│   │   │
│   │   ├── jobs/
│   │   │   ├── __init__.py
│   │   │   ├── queue.py               # Job registry & state
│   │   │   └── worker.py              # Background solver execution
│   │   │
│   │   ├── solver/
│   │   │   ├── __init__.py
│   │   │   ├── model.py               # CP-SAT model builder
│   │   │   ├── constraints.py         # Individual constraint functions
│   │   │   ├── diagnostics.py         # Infeasibility analysis
│   │   │   └── preprocess.py          # Data validation & grid generation
│   │   │
│   │   ├── parsers/
│   │   │   ├── __init__.py
│   │   │   ├── csv_parser.py          # CSV → canonical JSON
│   │   │   └── excel_parser.py        # XLSX → canonical JSON
│   │   │
│   │   └── utils/
│   │       ├── __init__.py
│   │       ├── path_helper.py         # Relocatable path resolution
│   │       └── validators.py          # Schema validation
│   │
│   ├── requirements.txt
│   └── .env.example
│
├── shared/
│   └── schemas/
│       ├── school.schema.json
│       ├── class.schema.json
│       ├── teacher.schema.json
│       ├── subject.schema.json
│       ├── resource.schema.json
│       ├── constraints.schema.json
│       ├── solver_input.schema.json
│       └── solver_output.schema.json
│
├── data/
│   ├── raw_uploads/
│   ├── parsed/
│   └── generated/
│
└── README.md
```

---

## PHASE 1: FOUNDATION & PATH MANAGEMENT

### File: `server/app/config.py`

**Purpose**: Central configuration for all paths, ensuring relocatability.

**Requirements**:
- Use `pathlib.Path` exclusively
- Detect project root dynamically using `__file__`
- All paths relative to project root
- Environment variable support for overrides
- No absolute paths anywhere

**Must expose**:
```python
PROJECT_ROOT: Path
DATA_DIR: Path
RAW_UPLOADS_DIR: Path
PARSED_DIR: Path
GENERATED_DIR: Path
SCHEMAS_DIR: Path
SERVER_HOST: str
SERVER_PORT: int
SOLVER_TIMEOUT_SECONDS: int
```

### File: `server/app/utils/path_helper.py`

**Purpose**: Safe path operations.

**Must provide**:
- `resolve_data_path(relative_path: str) -> Path`
- `ensure_dir_exists(path: Path) -> Path`
- `get_schema_path(schema_name: str) -> Path`
- All functions must handle missing directories gracefully

---

## PHASE 2: JSON SCHEMAS (CANONICAL DATA CONTRACT)

### Location: `shared/schemas/*.schema.json`

You must create **8 JSON schemas** that define the exact structure of all data.

### `school.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["school_id", "name", "start_time", "end_time", "weekdays", "periods_per_weekday"],
  "properties": {
    "school_id": {"type": "integer"},
    "name": {"type": "string"},
    "start_time": {"type": "string", "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"},
    "end_time": {"type": "string", "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"},
    "weekdays": {
      "type": "array",
      "items": {"type": "string", "enum": ["Mon","Tue","Wed","Thu","Fri","Sat"]}
    },
    "periods_per_weekday": {"type": "integer", "minimum": 1, "maximum": 10},
    "saturday_periods": {"type": "integer", "minimum": 0, "maximum": 6},
    "prayer_enabled": {"type": "boolean"},
    "prayer_duration_minutes": {"type": "integer", "minimum": 0},
    "lunch_period_index": {"type": "integer", "minimum": 0},
    "recess_period_indices": {
      "type": "array",
      "items": {"type": "integer"}
    }
  }
}
```

### `teacher.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["teacher_id", "name", "subjects_can_teach", "min_periods_day", "max_periods_day"],
  "properties": {
    "teacher_id": {"type": "string"},
    "name": {"type": "string"},
    "subjects_can_teach": {
      "type": "array",
      "items": {"type": "string"}
    },
    "sections_assigned": {
      "type": "array",
      "items": {"type": "string"}
    },
    "min_periods_day": {"type": "integer", "minimum": 0},
    "max_periods_day": {"type": "integer", "minimum": 1},
    "min_periods_week": {"type": "integer", "minimum": 0},
    "max_periods_week": {"type": "integer", "minimum": 1},
    "max_consecutive_periods": {"type": "integer", "minimum": 1, "maximum": 5},
    "availability": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {"type": "integer"}
      }
    }
  }
}
```

### `class.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["section_id", "grade", "subject_teacher_map"],
  "properties": {
    "section_id": {"type": "string"},
    "grade": {"type": "integer", "minimum": 1, "maximum": 12},
    "section_name": {"type": "string"},
    "class_teacher_id": {"type": "string"},
    "subject_teacher_map": {
      "type": "object",
      "additionalProperties": {"type": "string"}
    },
    "language_block_enabled": {"type": "boolean"},
    "language_teachers": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
```

### `subject.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["subject_id", "name", "category", "min_per_week", "max_per_week"],
  "properties": {
    "subject_id": {"type": "string"},
    "name": {"type": "string"},
    "category": {
      "type": "string",
      "enum": ["core", "language", "leisure", "lab"]
    },
    "min_per_week": {"type": "integer", "minimum": 0},
    "max_per_week": {"type": "integer", "minimum": 1},
    "requires_block": {"type": "boolean"},
    "block_length": {"type": "integer", "minimum": 2},
    "requires_resource": {"type": "boolean"},
    "resource_type": {"type": "string"}
  }
}
```

### `resource.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["resource_id", "resource_type", "max_simultaneous_capacity"],
  "properties": {
    "resource_id": {"type": "string"},
    "resource_type": {"type": "string"},
    "max_simultaneous_capacity": {"type": "integer", "minimum": 1}
  }
}
```

### `constraints.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "prayer_enabled": {"type": "boolean"},
    "language_sync_enabled": {"type": "boolean"},
    "class_teacher_period_1": {"type": "boolean"},
    "no_subject_twice_daily": {"type": "boolean"},
    "core_morning_only": {"type": "boolean"},
    "substitution_reserve_count": {"type": "integer", "minimum": 0},
    "soft_weights": {
      "type": "object",
      "properties": {
        "balance_load": {"type": "number"},
        "minimize_gaps": {"type": "number"},
        "prefer_core_morning": {"type": "number"}
      }
    }
  }
}
```

### `solver_input.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["school", "classes", "teachers", "subjects", "constraints"],
  "properties": {
    "school": {"$ref": "school.schema.json"},
    "classes": {
      "type": "array",
      "items": {"$ref": "class.schema.json"}
    },
    "teachers": {
      "type": "array",
      "items": {"$ref": "teacher.schema.json"}
    },
    "subjects": {
      "type": "array",
      "items": {"$ref": "subject.schema.json"}
    },
    "resources": {
      "type": "array",
      "items": {"$ref": "resource.schema.json"}
    },
    "constraints": {"$ref": "constraints.schema.json"},
    "time_limit_seconds": {"type": "integer", "minimum": 10, "maximum": 300}
  }
}
```

### `solver_output.schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["status", "timetable"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["OPTIMAL", "FEASIBLE", "INFEASIBLE", "TIMEOUT"]
    },
    "timetable": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "period": {"type": "integer"},
              "subject_id": {"type": "string"},
              "teacher_id": {"type": "string"},
              "room_id": {"type": "string"}
            }
          }
        }
      }
    },
    "meta": {
      "type": "object",
      "properties": {
        "solve_time_seconds": {"type": "number"},
        "objective_value": {"type": "number"},
        "soft_violations": {"type": "integer"}
      }
    },
    "diagnostics": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {"type": "string"},
          "message": {"type": "string"},
          "suggestion": {"type": "string"}
        }
      }
    }
  }
}
```

---

## PHASE 3: SCHEMA VALIDATOR

### File: `server/app/utils/validators.py`

**Purpose**: Runtime validation against JSON schemas.

**Must provide**:
```python
def validate_solver_input(data: dict) -> tuple[bool, list[str]]
def validate_solver_output(data: dict) -> tuple[bool, list[str]]
def validate_school(data: dict) -> tuple[bool, list[str]]
def validate_teacher(data: dict) -> tuple[bool, list[str]]
def validate_class(data: dict) -> tuple[bool, list[str]]
def validate_subject(data: dict) -> tuple[bool, list[str]]
```

**Implementation**:
- Use `jsonschema` library
- Load schemas from `SCHEMAS_DIR` dynamically
- Return (is_valid, error_messages)
- Pretty-print validation errors with field names

---

## PHASE 4: FASTAPI APPLICATION

### File: `server/app/main.py`

**Requirements**:
- FastAPI app with CORS enabled
- All routes under `/api/v1/timetable/`
- Request validation using Pydantic models
- Exception handlers for all error types
- Startup/shutdown events for cleanup

**Endpoints to implement**:
```python
POST   /api/v1/timetable/solve      # Submit generation job
GET    /api/v1/timetable/status/{job_id}
GET    /api/v1/timetable/result/{job_id}
POST   /api/v1/timetable/validate   # Validate manual swap
POST   /api/v1/timetable/upload     # Demo data upload
```

**Server Configuration**:
- Host: `0.0.0.0` (configurable)
- Port: `8010` (configurable)
- Auto-reload: Development only
- Logging: JSON format with timestamps

---

## PHASE 5: JOB MANAGEMENT

### File: `server/app/jobs/queue.py`

**Purpose**: In-memory job registry (no Redis for demo).

**Must provide**:
```python
class JobStatus(Enum):
    QUEUED = "queued"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"

class Job:
    job_id: str
    status: JobStatus
    progress: int  # 0-100
    result: Optional[dict]
    error: Optional[str]
    created_at: datetime

class JobQueue:
    def create_job(solver_input: dict) -> str
    def get_job(job_id: str) -> Optional[Job]
    def update_status(job_id: str, status: JobStatus, progress: int)
    def store_result(job_id: str, result: dict)
    def store_error(job_id: str, error: str)
```

**Thread-safe**: Use `threading.Lock` for concurrent access.

### File: `server/app/jobs/worker.py`

**Purpose**: Background solver execution.

**Must provide**:
```python
def execute_solver_job(job_id: str, solver_input: dict):
    """
    1. Update status to RUNNING
    2. Call solver
    3. Store result or error
    4. Update status to COMPLETED/FAILED
    """
```

**Execution**: Use `threading.Thread` (not multiprocessing for demo simplicity).

---

## PHASE 6: SOLVER PREPROCESSING

### File: `server/app/solver/preprocess.py`

**Purpose**: Transform solver_input into CP-SAT-ready structures.

**Must provide**:
```python
def generate_period_grid(school: dict) -> list[dict]:
    """
    Returns: [
        {day: "Mon", period: 1, start: "08:00", end: "08:40", type: "academic"},
        {day: "Mon", period: 2, start: "08:40", end: "09:20", type: "academic"},
        {day: "Mon", period: 3, start: "09:20", end: "10:00", type: "recess"},
        ...
    ]
    """

def build_teacher_capability_matrix(teachers, subjects) -> dict:
    """teacher_id -> set of subject_ids they can teach"""

def build_availability_mask(teacher: dict, period_grid: list) -> dict:
    """(day, period) -> bool (available or not)"""

def validate_feasibility(solver_input: dict) -> tuple[bool, list[str]]:
    """
    Pre-flight checks:
    - Sum of min_periods_per_week <= available slots
    - Teachers have capacity for assigned subjects
    - Block periods have valid start positions
    Returns: (is_feasible, warnings)
    """
```

---

## PHASE 7: CP-SAT SOLVER IMPLEMENTATION

### File: `server/app/solver/model.py`

**Purpose**: Core constraint programming model.

**Critical Design Principles**:
1. **Global Solve**: All classes solved together in one model
2. **Variable Naming**: Use clear prefixes (X_, T_, R_, B_)
3. **Objective Function**: Weighted sum of soft constraint penalties
4. **Time Limit**: Configurable, default 30 seconds

**Must implement**:

```python
from ortools.sat.python import cp_model
from typing import Dict, List, Tuple

class TimetableSolver:
    def __init__(self, solver_input: dict):
        self.model = cp_model.CpModel()
        self.solver_input = solver_input
        self.variables = {}
        self.constraints_map = {}

    def build_model(self):
        """
        1. Create decision variables
        2. Add hard constraints
        3. Add soft constraints (with penalties)
        4. Set objective
        """

    def solve(self, time_limit: int = 30) -> dict:
        """
        Returns solver_output schema-compliant result
        """

    def extract_timetable(self, solver) -> dict:
        """
        Convert CP-SAT solution to timetable JSON
        """
```

**Variable Structure**:
```python
# Binary variables
X[section_id][day][period][subject_id] = 0 or 1
T[section_id][day][period][teacher_id] = 0 or 1
R[section_id][day][period][resource_id] = 0 or 1

# Block start indicators
B[section_id][subject_id][day][period] = 0 or 1

# Soft constraint slack variables
slack_teacher_balance[teacher_id] = integer >= 0
slack_gaps[teacher_id] = integer >= 0
```

**Objective Function**:
```python
minimize:
    w1 * sum(slack_teacher_balance) +
    w2 * sum(slack_gaps) +
    w3 * sum(core_after_lunch_violations) +
    w4 * sum(pe_early_violations)
```

---

## PHASE 8: CONSTRAINT IMPLEMENTATIONS

### File: `server/app/solver/constraints.py`

**Purpose**: Individual constraint functions (one per constraint).

Each function must:
- Accept: `model`, `variables`, `solver_input`, `period_grid`
- Return: `None` (adds constraints to model directly)
- Be independently testable
- Handle toggle state from `solver_input.constraints`

**Must implement these functions**:

```python
def add_teacher_single_assignment_constraint(model, variables, teachers, period_grid):
    """
    For each teacher T and (day, period):
        sum over all sections: T[section][day][period][teacher_id] <= 1
    """

def add_max_consecutive_constraint(model, variables, teachers, period_grid):
    """
    For each teacher, for any window of (max_consecutive + 1) periods:
        sum of assignments <= max_consecutive
    """

def add_language_sync_constraint(model, variables, classes, period_grid):
    """
    If language_sync_enabled:
        For each class with language_block_enabled:
            For the designated language period:
                All language_teachers must be assigned
    """

def add_block_period_constraint(model, variables, subjects, period_grid):
    """
    For subjects requiring block periods:
        Use B[section][subject][day][start_period] indicator
        Force X[section][day][period] = 1 for next k periods
        Ensure no crossing of lunch/recess
    """

def add_resource_capacity_constraint(model, variables, resources, period_grid):
    """
    For each resource and (day, period):
        sum over sections using resource <= capacity
    """

def add_subject_frequency_constraint(model, variables, classes, subjects):
    """
    For each (section, subject):
        min_per_week <= sum of X[section][day][period][subject] <= max_per_week
    """

def add_teacher_load_bounds_constraint(model, variables, teachers, period_grid):
    """
    For each teacher:
        Daily: min_periods_day <= assignments <= max_periods_day
        Weekly: min_periods_week <= assignments <= max_periods_week
    """

def add_substitution_reserve_constraint(model, variables, teachers, period_grid, reserve_count):
    """
    For each period:
        count of unassigned teachers >= reserve_count
    """

def add_class_teacher_period_1_constraint(model, variables, classes, period_grid):
    """
    If enabled:
        For each class with class_teacher_id:
            T[section]["Mon"][1][class_teacher_id] = 1
            (and same for other days)
    """

# Soft constraints (return slack variables)

def add_core_morning_preference(model, variables, subjects, period_grid) -> list:
    """
    For core subjects scheduled after lunch:
        Create slack variable
    Returns: list of slack variables
    """

def add_teacher_balance_preference(model, variables, teachers, period_grid) -> list:
    """
    Minimize variance of teacher daily loads
    Returns: list of slack variables
    """

def add_minimize_gaps_preference(model, variables, teachers, period_grid) -> list:
    """
    Penalize gaps in teacher schedules
    Returns: list of slack variables
    """
```

**CRITICAL**: Every constraint must check its toggle state:
```python
if solver_input["constraints"].get("language_sync_enabled", True):
    # add constraint
```

---

## PHASE 9: DIAGNOSTICS & INFEASIBILITY ANALYSIS

### File: `server/app/solver/diagnostics.py`

**Purpose**: Generate human-readable explanations when solver fails.

**Must provide**:
```python
def analyze_infeasibility(solver_input: dict, solver_status: str) -> list[dict]:
    """
    Returns: [
        {
            "type": "TEACHER_OVERLOAD",
            "message": "Teacher T001 (Priya Sharma) assigned 35 periods but max_week=30",
            "suggestion": "Increase max_periods_week to 40 or reassign subjects"
        },
        {
            "type": "INSUFFICIENT_RESOURCES",
            "message": "Physics Lab capacity is 2 but 4 sections need it at Mon P3",
            "suggestion": "Stagger physics lab periods or add 2 more lab slots"
        }
    ]
    """

def detect_overloaded_teachers(solver_input: dict) -> list[dict]:
    """Check if required periods exceed teacher capacity"""

def detect_resource_bottlenecks(solver_input: dict) -> list[dict]:
    """Check if resource demand exceeds supply"""

def detect_impossible_blocks(solver_input: dict, period_grid: list) -> list[dict]:
    """Check if block periods can fit in grid"""

def suggest_relaxations(diagnostics: list[dict]) -> list[str]:
    """
    Returns prioritized list of suggestions:
    1. "Disable language_sync_enabled constraint"
    2. "Reduce min_per_week for subject MATH from 6 to 5"
    3. "Increase max_consecutive for Teacher T003 from 3 to 4"
    """
```

---

## PHASE 10: API ENDPOINTS IMPLEMENTATION

### File: `server/app/api/solve.py`

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
import uuid

router = APIRouter()

class SolveRequest(BaseModel):
    school: dict
    classes: list[dict]
    teachers: list[dict]
    subjects: list[dict]
    resources: list[dict]
    constraints: dict
    time_limit_seconds: int = 30

@router.post("/solve")
async def solve_timetable(request: SolveRequest):
    """
    1. Validate solver_input schema
    2. Create job in JobQueue
    3. Start worker thread
    4. Return job_id immediately
    """
    # Validation
    solver_input = request.dict()
    is_valid, errors = validate_solver_input(solver_input)
    if not is_valid:
        raise HTTPException(400, {"errors": errors})

    # Pre-flight feasibility check
    is_feasible, warnings = validate_feasibility(solver_input)
    if not is_feasible:
        return {
            "status": "rejected",
            "warnings": warnings,
            "suggestion": "Fix data or relax constraints"
        }

    # Create job
    job_id = str(uuid.uuid4())
    job_queue.create_job(job_id, solver_input)

    # Start worker
    worker_thread = Thread(target=execute_solver_job, args=(job_id, solver_input))
    worker_thread.start()

    return {"job_id": job_id, "status": "queued"}
```

### File: `server/app/api/status.py`

```python
@router.get("/status/{job_id}")
async def get_job_status(job_id: str):
    job = job_queue.get_job(job_id)
    if not job:
        raise HTTPException(404, "Job not found")

    return {
        "job_id": job_id,
        "status": job.status.value,
        "progress": job.progress,
        "created_at": job.created_at.isoformat()
    }
```

### File: `server/app/api/result.py`

```python
@router.get("/result/{job_id}")
async def get_result(job_id: str):
    job = job_queue.get_job(job_id)
    if not job:
        raise HTTPException(404, "Job not found")

    if job.status != JobStatus.COMPLETED:
        return {
            "status": job.status.value,
            "message": "Job not yet completed"
        }

    # Write to file
    output_path = GENERATED_DIR / f"{job_id}_timetable.json"
    with open(output_path, "w") as f:
        json.dump(job.result, f, indent=2)

    return job.result
```

### File: `server/app/api/validate.py`

```python
class SwapRequest(BaseModel):
    job_id: str
    section_id: str
    day: str
    period_from: int
    period_to: int

@router.post("/validate")
async def validate_swap(request: SwapRequest):
    """
    1. Load existing timetable from job result
    2. Apply proposed swap
    3. Check hard constraints only (no re-solve)
    4. Return valid/invalid + reasons
    """
    job = job_queue.get_job(request.job_id)
    if not job or not job.result:
        raise HTTPException(404, "Timetable not found")

    timetable = job.result["timetable"]

    # Apply swap
    temp_timetable = apply_swap(timetable, request)

    # Validate
    violations = check_hard_constraints(temp_timetable, job.solver_input)

    if violations:
        return {
            "valid": False,
            "violations": violations
        }

    return {"valid": True}
```

### File: `server/app/api/upload.py`

```python
from fastapi import UploadFile, File

@router.post("/upload")
async def upload_demo_data(file: UploadFile = File(...), data_type: str = "teachers"):
    """
    1. Save to raw_uploads/
    2. Parse CSV/XLSX
    3. Validate against schema
    4. Save to parsed/
    5. Return parsed data + manifest_id
    """
    # Save raw file
    file_id = str(uuid.uuid4())
    raw_path = RAW_UPLOADS_DIR / f"{file_id}_{file.filename}"

    with open(raw_path, "wb") as f:
        f.write(await file.read())

    # Parse
    if file.filename.endswith(".csv"):
        parsed = parse_csv(raw_path, data_type)
    elif file.filename.endswith(".xlsx"):
        parsed = parse_excel(raw_path, data_type)
    else:
        raise HTTPException(400, "Unsupported format")

    # Validate
    validator = get_validator(data_type)
    is_valid, errors = validator(parsed)
    if not is_valid:
        return {
            "status": "error",
            "errors": errors
        }

    # Save parsed
    parsed_path = PARSED_DIR / f"{file_id}_{data_type}.json"
    with open(parsed_path, "w") as f:
        json.dump(parsed, f, indent=2)

    return {
        "status": "success",
        "manifest_id": file_id,
        "parsed_count": len(parsed)
    }
```

---

## PHASE 11: DATA PARSERS

### File: `server/app/parsers/csv_parser.py`

**Must handle**:
- Teachers CSV
- Classes CSV
- Subjects CSV
- Resources CSV

**Format expectations**:
```
teachers.csv:
teacher_id,name,subjects,min_day,max_day,max_consecutive
T001,Priya Sharma,"MATH;PHYSICS",2,6,3

classes.csv:
section_id,grade,class_teacher_id,subjects
8A,8,T001,"MATH:T001;PHYSICS:T002;KANNADA:T003"

subjects.csv:
subject_id,name,category,min_week,max_week,requires_block,block_length,resource_type
MATH,Mathematics,core,6,7,false,0,
PHYSICS_LAB,Physics Lab,lab,2,2,true,2,Physics Lab
```

**Must implement**:
```python
import csv
from pathlib import Path

def parse_csv(file_path: Path, data_type: str) -> list[dict]:
    """
    Parse CSV and return list of dicts matching schema
    Handle multi-value fields (split by ; or ,)
    Handle key:value mappings (e.g., "MATH:T001;PHYSICS:T002")
    """

def parse_teachers_csv(file_path: Path) -> list[dict]:
    """
    Parse teachers.csv → list of teacher objects
    Handle availability if provided (optional columns)
    """

def parse_classes_csv(file_path: Path) -> list[dict]:
    """
    Parse classes.csv → list of class objects
    Parse subject_teacher_map from "SUBJECT:TEACHER;SUBJECT:TEACHER"
    """

def parse_subjects_csv(file_path: Path) -> list[dict]:
    """
    Parse subjects.csv → list of subject objects
    Convert string booleans to actual bools
    """
```

### File: `server/app/parsers/excel_parser.py`

**Must handle**: Same as CSV but using `openpyxl` or `pandas`

```python
import openpyxl
from pathlib import Path

def parse_excel(file_path: Path, data_type: str) -> list[dict]:
    """
    Parse XLSX file using openpyxl
    Support multiple sheets (one per data type)
    """
```

---

## PHASE 12: REQUIREMENTS.TXT

### File: `server/requirements.txt`

```txt
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
ortools==9.8.3296
jsonschema==4.20.0
python-multipart==0.0.6
openpyxl==3.1.2
pandas==2.1.4
python-dotenv==1.0.0
```

---

## PHASE 13: README.MD

### File: `README.md`

```markdown
# Timetable Generator Backend

Production-grade constraint-based timetable generator using Google OR-Tools CP-SAT.

## Features

- ✅ One global solve (all classes together)
- ✅ Relocatable design (no hardcoded paths)
- ✅ Schema-first validation
- ✅ Async job execution
- ✅ Infeasibility diagnostics
- ✅ Manual swap validation

## Setup

### 1. Create Virtual Environment

```bash
cd server
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

### 2. Install Dependencies

```bash
pip install -r requirements.txt
```

### 3. Environment Configuration

Copy `.env.example` to `.env`:

```bash
cp .env.example .env
```

Edit `.env`:
```
SERVER_HOST=0.0.0.0
SERVER_PORT=8010
SOLVER_TIMEOUT=30
```

### 4. Run Server

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8010
```

Server will start at `http://localhost:8010`

## API Endpoints

### POST `/api/v1/timetable/solve`
Submit timetable generation job.

**Request Body**: See `shared/schemas/solver_input.schema.json`

**Response**:
```json
{
  "job_id": "uuid",
  "status": "queued"
}
```

### GET `/api/v1/timetable/status/{job_id}`
Check job status.

**Response**:
```json
{
  "job_id": "uuid",
  "status": "running",
  "progress": 45
}
```

### GET `/api/v1/timetable/result/{job_id}`
Get generated timetable.

**Response**: See `shared/schemas/solver_output.schema.json`

### POST `/api/v1/timetable/validate`
Validate manual swap.

**Request Body**:
```json
{
  "job_id": "uuid",
  "section_id": "8A",
  "day": "Mon",
  "period_from": 2,
  "period_to": 4
}
```

### POST `/api/v1/timetable/upload`
Upload demo data (CSV/XLSX).

**Form Data**:
- `file`: CSV or XLSX file
- `data_type`: "teachers" | "classes" | "subjects" | "resources"

## Testing

```bash
# Test solver with sample data
python -m app.solver.model --test

# Validate schemas
python -m app.utils.validators --test
```

## Project Structure

```
server/
├── app/
│   ├── main.py              # FastAPI app
│   ├── config.py            # Configuration
│   ├── api/                 # HTTP endpoints
│   ├── jobs/                # Job queue
│   ├── solver/              # CP-SAT solver
│   ├── parsers/             # CSV/XLSX parsers
│   └── utils/               # Helpers
└── requirements.txt
```

## Relocatability

This backend is designed to work anywhere. All paths are relative to project root.

**Current location**: `/Applications/Projects/Timetable`
**Future location**: `/Applications/Projects/SchoolOS/School-OS`

No code changes needed when relocating.
```

---

## PHASE 14: TESTING & VALIDATION

### Create `server/app/test_solver.py`

```python
"""
Manual test script to verify solver works end-to-end
"""
import json
from pathlib import Path
from app.solver.model import TimetableSolver
from app.solver.preprocess import validate_feasibility

def test_sample_school():
    """Test with minimal valid input"""

    solver_input = {
        "school": {
            "school_id": 1,
            "name": "Demo School",
            "start_time": "08:00",
            "end_time": "14:00",
            "weekdays": ["Mon", "Tue", "Wed", "Thu", "Fri"],
            "periods_per_weekday": 6,
            "saturday_periods": 0,
            "prayer_enabled": True,
            "prayer_duration_minutes": 30,
            "lunch_period_index": 4,
            "recess_period_indices": [2]
        },
        "classes": [
            {
                "section_id": "8A",
                "grade": 8,
                "class_teacher_id": "T001",
                "subject_teacher_map": {
                    "MATH": "T001",
                    "SCIENCE": "T002",
                    "ENGLISH": "T003"
                },
                "language_block_enabled": False,
                "language_teachers": []
            }
        ],
        "teachers": [
            {
                "teacher_id": "T001",
                "name": "Teacher 1",
                "subjects_can_teach": ["MATH"],
                "min_periods_day": 2,
                "max_periods_day": 6,
                "min_periods_week": 10,
                "max_periods_week": 30,
                "max_consecutive_periods": 3,
                "availability": {}
            },
            {
                "teacher_id": "T002",
                "name": "Teacher 2",
                "subjects_can_teach": ["SCIENCE"],
                "min_periods_day": 2,
                "max_periods_day": 6,
                "min_periods_week": 10,
                "max_periods_week": 30,
                "max_consecutive_periods": 3,
                "availability": {}
            },
            {
                "teacher_id": "T003",
                "name": "Teacher 3",
                "subjects_can_teach": ["ENGLISH"],
                "min_periods_day": 2,
                "max_periods_day": 6,
                "min_periods_week": 10,
                "max_periods_week": 30,
                "max_consecutive_periods": 3,
                "availability": {}
            }
        ],
        "subjects": [
            {
                "subject_id": "MATH",
                "name": "Mathematics",
                "category": "core",
                "min_per_week": 5,
                "max_per_week": 6,
                "requires_block": False,
                "block_length": 0,
                "requires_resource": False,
                "resource_type": None
            },
            {
                "subject_id": "SCIENCE",
                "name": "Science",
                "category": "core",
                "min_per_week": 4,
                "max_per_week": 5,
                "requires_block": False,
                "block_length": 0,
                "requires_resource": False,
                "resource_type": None
            },
            {
                "subject_id": "ENGLISH",
                "name": "English",
                "category": "language",
                "min_per_week": 4,
                "max_per_week": 5,
                "requires_block": False,
                "block_length": 0,
                "requires_resource": False,
                "resource_type": None
            }
        ],
        "resources": [],
        "constraints": {
            "prayer_enabled": True,
            "language_sync_enabled": False,
            "class_teacher_period_1": False,
            "no_subject_twice_daily": True,
            "core_morning_only": True,
            "substitution_reserve_count": 0,
            "soft_weights": {
                "balance_load": 5.0,
                "minimize_gaps": 3.0,
                "prefer_core_morning": 10.0
            }
        },
        "time_limit_seconds": 30
    }

    # Validate
    is_feasible, warnings = validate_feasibility(solver_input)
    print(f"Feasibility check: {is_feasible}")
    if warnings:
        print("Warnings:", warnings)

    # Solve
    solver = TimetableSolver(solver_input)
    result = solver.solve(time_limit=30)

    print(f"\nSolver status: {result['status']}")
    print(f"Solve time: {result['meta']['solve_time_seconds']:.2f}s")

    if result['status'] in ['OPTIMAL', 'FEASIBLE']:
        print("\n✅ Timetable generated successfully!")
        print(json.dumps(result['timetable']['8A'], indent=2))
    else:
        print("\n❌ Solver failed")
        print(json.dumps(result['diagnostics'], indent=2))

if __name__ == "__main__":
    test_sample_school()
```

---

## CRITICAL IMPLEMENTATION RULES

### 1. PATH MANAGEMENT
```python
# ❌ NEVER DO THIS
file_path = "/Applications/Projects/Timetable/data/output.json"

# ✅ ALWAYS DO THIS
from app.config import DATA_DIR
file_path = DATA_DIR / "output.json"
```

### 2. SCHEMA VALIDATION
```python
# ❌ NEVER DO THIS
def process_data(data):
    # Assume data is valid
    result = data['school']['name']

# ✅ ALWAYS DO THIS
def process_data(data):
    is_valid, errors = validate_solver_input(data)
    if not is_valid:
        raise ValueError(f"Invalid input: {errors}")
    result = data['school']['name']
```

### 3. ERROR HANDLING
```python
# ❌ NEVER DO THIS
result = solver.Solve(model)
return result

# ✅ ALWAYS DO THIS
try:
    result = solver.Solve(model)
    if result == cp_model.INFEASIBLE:
        diagnostics = analyze_infeasibility(solver_input, result)
        return {"status": "INFEASIBLE", "diagnostics": diagnostics}
    return extract_solution(solver)
except Exception as e:
    return {"status": "ERROR", "message": str(e)}
```

### 4. CONSTRAINT TOGGLES
```python
# ❌ NEVER DO THIS
def add_language_sync(model, variables):
    # Always add constraint

# ✅ ALWAYS DO THIS
def add_language_sync(model, variables, solver_input):
    if not solver_input["constraints"].get("language_sync_enabled", True):
        return  # Skip this constraint
    # Add constraint
```

### 5. LOGGING
```python
# ✅ ALWAYS LOG CRITICAL OPERATIONS
import logging

logger = logging.getLogger(__name__)

def solve():
    logger.info(f"Starting solve for job {job_id}")
    try:
        result = solver.solve()
        logger.info(f"Solve completed: {result['status']}")
        return result
    except Exception as e:
        logger.error(f"Solve failed: {e}", exc_info=True)
        raise
```

---

## DELIVERABLES CHECKLIST

After implementation, verify:

- [ ] All 8 JSON schemas created and valid
- [ ] `config.py` uses pathlib.Path exclusively
- [ ] All 5 API endpoints implemented and tested
- [ ] Job queue supports QUEUED → RUNNING → COMPLETED/FAILED
- [ ] CP-SAT model implements all hard constraints
- [ ] Soft constraints added to objective function
- [ ] Diagnostics module suggests relaxations
- [ ] CSV/XLSX parsers handle all data types
- [ ] Schema validation runs on all inputs
- [ ] Test script runs successfully
- [ ] Server starts without errors on port 8010
- [ ] README.md is complete and accurate
- [ ] No absolute paths anywhere in code
- [ ] All files use relative imports
- [ ] Error messages are human-readable

---

## TESTING COMMANDS

```bash
# Start server
cd server
source venv/bin/activate
uvicorn app.main:app --reload --port 8010

# In another terminal - test solve endpoint
curl -X POST http://localhost:8010/api/v1/timetable/solve \
  -H "Content-Type: application/json" \
  -d @test_input.json

# Check status
curl http://localhost:8010/api/v1/timetable/status/{job_id}

# Get result
curl http://localhost:8010/api/v1/timetable/result/{job_id}
```

---

## COMMON PITFALLS TO AVOID

1. **Hardcoded paths**: Use `config.py` constants
2. **Missing schema validation**: Validate at API boundary
3. **Blocking API calls**: Use background jobs
4. **Poor error messages**: Return actionable suggestions
5. **Ignoring constraint toggles**: Always check toggle state
6. **Sequential class solving**: Solve all classes together
7. **Missing preprocessing**: Validate feasibility before solving
8. **No diagnostics**: Always explain why solver failed
9. **Absolute imports**: Use relative imports within app
10. **Missing type hints**: Use type hints for clarity

---

## SUCCESS CRITERIA

Your implementation is complete when:

1. ✅ Server starts successfully on port 8010
2. ✅ Test script generates a valid timetable in < 30 seconds
3. ✅ All hard constraints are satisfied in output
4. ✅ Infeasible input produces helpful diagnostics
5. ✅ Manual swap validation works correctly
6. ✅ CSV upload parses and validates data
7. ✅ No errors in logs during normal operation
8. ✅ Code can be moved to new location without changes
9. ✅ All endpoints return schema-compliant responses
10. ✅ README instructions work for a new developer

---

## FINAL NOTES

This is a **demo for school principals**. Every feature must work flawlessly. No placeholders, no TODOs, no "coming soon".

The solver is the core. Everything else serves it.

Focus on correctness first, optimization second.

When in doubt, choose the simpler implementation that works over the clever one that might fail.

**Good luck. Build something principals will trust.**
